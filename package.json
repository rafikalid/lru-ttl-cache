{
  "name": "lru-ttl-cache-monorepo",
  "version": "4.0.0",
  "description": "Fast LRU and TTL cache with upsert and promise option",
  "repository": "https://github.com/rafikalid/lru-ttl-cache.git",
  "author": "khalid rafik <khalid.rfk@gmail.com>",
  "license": "MIT",
  "packageManager": "yarn@4.12.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -pt run build",
    "test": "yarn workspaces foreach -pt run test",
    "bench": "yarn workspace benchmarks run bench"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
